# PLACEHOLDER -- not a real credential.
#
# Replace this file's contents with the real IMAP password for
# {{ agent_mail_address }} before the mbsync timer can authenticate. Either:
#
#   1. supply an ansible-vault-encrypted `agent_mail_imap_password` variable and
#      re-run the agent-setup playbook (preferred, keeps the host reproducible), or
#   2. replace this file's contents by hand on the host:
#        printf '%s' 'the-real-password' > {{ agent_mail_credential_dir }}/imap_password
#        chmod 0600 {{ agent_mail_credential_dir }}/imap_password
#        chown {{ agent_mail_username }}:{{ agent_mail_username }} {{ agent_mail_credential_dir }}/imap_password
#
# mbsync reads this file's raw content via PassCmd ("cat ...imap_password"); do not
# add a trailing comment or blank line after the real password.
CHANGE-ME-REPLACE-WITH-REAL-HETZNER-MAILBOX-PASSWORD
